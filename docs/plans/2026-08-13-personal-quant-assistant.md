# 个人量化助手收口计划

状态：**唯一现行计划**（2026-08-13）。  
产品名：个人量化助手。编排层是 Hermes-quant-agent，领域后端是 ai-quant-platform。

`D-31` / `D-32` / `D-33` / `D-34` **不再是产品线、不再是默认入口、不再是下一切片名**。  
那些字符串只许出现在：（1）已经落地的代码/表/LaunchAgent 路径，（2）盖了「历史文件」印的旧文档。  
下一轮施工禁止再开 `D-35`。要加能力，改这一份，或另写一份并在 `docs/README.md` 里**替换**本文，不要并列。

## 1. 这台助手是干什么的

本机、单用户、模拟盘。每天做且只做五件事：

1. **盯盘**：行情、雷达、账户、异常，开口之前答案已在。
2. **复现**：论文或明确假设 → 可证伪因子/策略，收据说话。
3. **回测**：真数据、可复现实验命名空间，一次一收据。
4. **模拟盘每天跑**：只有你**挂上**的因子/策略自动跑信号和模拟成交。双引擎通过只进「已验证候选」，不等于挂上。
5. **看效果**：P&L、回撤、诚实失败写进明早摘要。失败假设要留下，明天不许装作没死过。

不是：发布级 Web Chat 产品、公开 composer、第三条研究厂、LLM 每天辩论下单、实盘。

## 2. 什么自动、什么算你已授权

值班、已挂上的模拟策略、评价：**始终自动**。没上实盘，这三件事不要被发布仪式或 live 级 kill switch 挡住。

研究分两截，禁止合成一句：

| 你做了什么 | 助手最多自动做到哪 | 默认停在哪 |
|---|---|---|
| 发来**论文、链接、截图、书面策略说明** | 读材料 → 代码 → 双引擎回测（平台回测器 + Qlib） | **已验证候选**。要进每天跑，须你说「挂上」，或你事先写明「过了就挂」 |
| 给一条还有效的**研究方向**（「持续挖美股量价反转」） | 提案或跑研究链；**同时最多 1 个作业** | 仍停在已验证候选。方向**不是**全权委托找材料、写码、过了自己挂仓 |
| 发来**视频** | 先抽规则。多数停下来问你 | 不构成第 4 步完成定义，不做视频 intake 主路径 |
| 什么都没给 | 禁止发明周期 | 只维护已挂策略 |

没有授权：五分钟 worker 不得发明研究作业。  
「过了就挂」必须是那一次材料或那一条方向上的明文，不能从「发了论文」推出来。

提取不出可证伪公式、股票池你没给、或对不上收据：停下来问你，或诚实失败。禁止用模型散文冒充「读过 / 回测过」。禁止用视频口播编一条因子再进候选。

「权限放到最开」仍不适用于：未挂上的因子进常驻模拟、LLM 直接改注册表、任何 live 路径。

## 3. 一个产品，不是一个页面

这是**一个助手**，不是三套厂。不是「只剩 `/hermes` 四个格子」。

- **聊天是万能遥控**：网页 Hermes、Discord，以后可以再接微信。底层是同一台 Hermes，能做的事比「复现一篇论文」多——查仓、解释盈亏、暂停某条模拟策略、跑一次指定回测、问雷达、问某只票的新闻。多出来的事必须落在值班/研究/模拟这三本账上，用已有工具，给收据。不要靠聊天再长出第四套产品。
- **平台页是手，继续一等**：因子实验室、回测器、模拟盘、雷达、持仓地图都留着。聊天调它们，不取代它们。
- **三本账**：值班账（摘要/雷达/新闻）、研究账（材料、作业、**已验证候选**）、模拟账（**已挂上**的策略 + 限额试运行仓）。来源不再决定跑哪本账。候选不是挂上。

通道：用已经接好的 Hermes 网页和 Discord。不要做临时网页 chat、Discord/TUI 桥、直连 Hermes 作曲家。微信是后接适配，不挡主线。

## 4. 现在已经有、但必须收口的零件

这些能力留下，产品身份拆掉：

- 真 Futu 行情、回测器、因子实验室、模拟账户、策略仓
- 论文入队机（旧称 D-33）和双引擎研究作业（旧称 D-34）都变成研究账的**来源适配器**
- 纸面试运行仓留下，改叫试运行仓；不再按来源分两套默认入口
- 收据、双引擎对照、`paper_only` 进不了 live registry：留下
- 本机 LaunchAgent 栈留下；服务寿命不绑 AI 终端

这些先停，不再当 NEXT：

- Agent v0.2 公开 composer / 发布 Gate / 三仓干净 HEAD 候选仪式
- quark P2/P3、再开一条研究引擎、再开一条平行晨报
- 为了把 soak `1/10` 做成 `10/10` 而放宽对照或伪造观察
- 把 TradingAgents 式多空辩论当日常交易员

## 5. 施工顺序（通过本文之后才许动代码）

隔离施工：`/Users/sunyibo/programs/.worktrees/coo-unify/` 的 `refactor/coo-unify`，空库 `quantplatform_coo`。  
live `:3001` / 正式 `quantplatform` / 已有试运行仓观察，没有单独授权不要切。

| 步 | 开不开 | 做什么 | 完成的样子 |
|---|---|---|---|
| 0 | 已完成 | 本文 + 文档收口 | `docs/README.md` 只指向本文；旧 D-xx 计划盖历史印 |
| 1 | **已完成（隔离语义）** | 解冻纸面观察 | 隔离栈上，已挂上的 `automation_managed + paper_only + allocated` 仓能在 `kill_switch=true` 时成交并记账；live 仍关。预览种子成交不是每日观察，种子市值差不是策略效果 |
| 2 | **进行中（隔离语义）** | 聊天当遥控 | 派研究和挂仓是两条命令。双引擎默认停在已验证候选；只有「挂上」或该次明文「过了就挂」才进每天跑。隔离预览 `:3002` 遥控面板可核对。不是完整研究账，也不是助手已经在看效果 |
| 3 | 先别开 | 一本模拟账 | 取决于「已验证候选」和「已挂上」 |
| 4 | 先别开 | 一本研究账 | 论文/说明 → 双引擎 → **已验证候选**；视频只抽规则、多数停问；持续方向最多 1 作业且停在候选 |
| 5 | 跟在第 1 步后 | 评价进摘要 | 第 1 步解冻后才有东西可写 |

每做完一步：neat-freak 只改**现行**文档（本文、README、指南、运维），不要往历史计划里追加「现已上线」。

## 6. 红线（下次写代码时看不到就会再跑偏）

- `live_trading_enabled=false`。任何因子、试运行仓、收据都没有 live 升级操作。
- 常驻模拟只跑**已挂上**的因子。已验证候选、候选文件、模型散文、ticker 像不像，都不能进每天跑。
- 双引擎通过只进**已验证候选**。进每天跑必须另有「挂上」，或该次材料/方向上明文「过了就挂」。
- 持续方向最多同时 1 个研究作业，默认停在候选，不得自己找材料、过了自己挂仓。
- 没有授权：禁止发明研究作业，只维护已挂策略。没有「每周槽」。
- 视频不是第 4 步完成条件。先抽规则，多数停下来问。禁止口播编因子进候选。
- 模型说搜了网 / 读了论文 / 看了视频 / 回测过 / 成交了，必须有无正文收据；没有就失败，不许 `mark_succeeded`。
- 公式或股票池抽不出来：停下来问你，或诚实失败。禁止推断宇宙。
- 不重放已经 apply 过的正式库 migration（028–033 等）。新库 `quantplatform_coo` 从 001 顺序来。
- 不改部署镜像 `data/_runtime/agent-v02-work/`。不自动推 `main`。
- 手工 Scene-B 和任何 **live** 资格仍走 Gate 1/2/3。纸面每天跑不走这三道人闸。
- 四只 ETF 上的好看回测不是横截面结论。扩大宇宙是研究账的事，不是另开产品。

## 7. 代码化石（先别改名，以免更乱）

允许暂时存在、禁止当成产品宣传：

- 目录/模块：`quant_system/d34/`、`hqa/factor_automation*`
- 表：`d34_mandates`、`d34_experiment_jobs`、`d34_canaries`…
- LaunchAgent：`com.aiquant.d34-worker`、factor-automation driver
- 路由：`/api/hermes/mandates`、`/api/hermes/canaries`

对外文案用：研究作业、试运行仓、模拟账、研究账。  
改名是第 2–4 步做完以后的清洁工，不是现在的主线。

## 8. 明确不做

- 不在这篇里重写整个前端视觉系统
- 不新开 Claude Code / Codex 会话当施工队
- 不把 live soak 金丝雀迁进 `quantplatform_coo`
- 不把公开 Web Chat 做成下一步
- 不把 quark / 期权 / Polymarket 提升成助手主线

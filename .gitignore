.venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.tmp-pytest/
.tmp*/
.codegraph/
logs/*.jsonl
review/entries.jsonl
review/entries.md
predictions/
opportunities/
market_foresight/
artifacts/
automation/
# Ignore runtime data, but track Scene-A threshold config (audit PR-5).
# Must use `data/*` (not `data/`) so negation of children works.
data/*
!data/_runtime/
data/_runtime/*
!data/_runtime/signal_thresholds.json
.DS_Store
.grok/

# Superpowers brainstorming runtime state. Keep durable HTML/SDD evidence, but
# never publish local bearer tokens, ports, process IDs, or server instances.
.superpowers/brainstorm/.last-*
.superpowers/brainstorm/*/state/

# Hermes F0/F1 platform token rebind — 2026-07-13

## Done
- Rebound `f0/shared.css` + `f1/prototype.css` to QUANTUM_CORE SSOT (`globals.css`).
- Surfaces: `#12110E` / `#181714` / `#23211C` / border `#302C25`; text `#E7E0D3` / `#9B9488` / mono `#D8D1C4`.
- Primary CTA + secondary outline → info `#5EA2FF` (not Binance yellow).
- Success `#089981`, danger `#F23645`, warning `#F0B90B` (safety strip + warn pills / attention edges only).
- Hermes `#9085E9` = brand mark + `:focus-visible` only; no purple atmosphere.
- `direction-a.html` hard-coded alert edges → `var(--warning)`; brand inset uses base rgba.
- README: F1 **approved** (craft + token rebind); platform alignment table dated 2026-07-13.
- Kept full-width desk density; no layout skinny-column regression.

## Commit (ai-quant-platform, no push)
`docs(frontend): align Hermes F0/F1 tokens with QUANTUM_CORE palette`

Preserved unstaged: `earnings_calendar.csv`, `.understand-anything/`.
